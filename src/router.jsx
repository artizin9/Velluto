import { createBrowserRouter } from "react-router-dom";

export const routes = createBrowserRouter([{
    path: '/',
    children: [{
        //  paht: '/auth/register',
        //  element: <Register />
    }]
}]);
